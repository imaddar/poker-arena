# API Service (placeholder)

Responsibilities:
- user creation + lightweight auth token
- agent registration + immutable version publish
- table create/join/state endpoints
- start/stop table loop controls

Implementation to follow in vertical slice.
